#include <stdio.h>

int main()
{
   char a[10001];
scanf("%s", a);
int count1=0,count2=0,count3=0;
for (int i = 0; i < '\0'; i++)
{
    if (<=a>=Z)
    {
       count1++;
    }
    
}
printf("%d", count1);
    return 0;
}